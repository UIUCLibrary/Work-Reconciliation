PREFIX bf: <http://id.loc.gov/ontologies/bibframe/>

SELECT COUNT(DISTINCT ?work)
WHERE {
  GRAPH <http://localhost:8890/test2> { 
    ?work rdf:type <http://id.loc.gov/ontologies/bibframe/Work> .
    FILTER(STRSTARTS(STR(?work),"http://example.org"))
  }
}