PREFIX bf: <http://id.loc.gov/ontologies/bibframe/>

SELECT COUNT(DISTINCT ?work)
WHERE {
  GRAPH <http://localhost:8890/test3> { 
    ?hub rdf:type <http://id.loc.gov/ontologies/bibframe/Hub> .
    ?hub bf:hasExpression ?work .
  }
}